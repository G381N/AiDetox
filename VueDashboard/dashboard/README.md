# Vue Dashboard Project

## Quick Start

Install and run:

```bash
npm install
npm run serve
```

For a component-by-component list of BootstrapVue concepts used (navbar first), see:

- `Documentation/BootstrapVue-Used-In-App.md`

For a full BootstrapVue reference, see:

- `BOOTSTRAP-VUE-COMPONENTS.md`
